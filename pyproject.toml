[project]
name = "elastic-annotation"
version = "0.1.0"
description = "Semi-automated pipeline to annotate Elasticsearch index fields for API documentation and MCP server."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
